# Cloud Storage Backend

Backend for a simple cloud storage system with authentication, JWT, file upload, and soft-delete.  
Tech stack: **Go (Fiber) + Postgres + pgx**.

---

## 🚀 Features

- User registration and authentication
- JWT access & refresh tokens
- Get current user info (`/me`)
- File upload
- View user files
- Soft-delete files (`deleted_at`)
- File filtering: images, deleted, normal files

---

## 🧩 Project Structure
